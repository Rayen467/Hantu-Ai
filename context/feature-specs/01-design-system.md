Read `AGENTS.md` before starting

we're adding the design system and UI primitive components

Install and configure `shadcn/ui`.

add these shadcn components:
- Button
- Card
- Dialog
- Input
- Tabs
- Textarea
- ScrollArea

Do not modify the generated `components/UI/*` after instantiation. 

Also install `lucide-react`. 

Create `lib/utils.ts ` with a reasuable `cn()` helper for merging Tailwind classes.

Ensure all components match the existing dark theme in `globals.css`.